---
- name: Create users files/dir in the users home dir
  hosts: all
  tasks:
    - name: create users
      user:
        name: "{{a}}"
        password: "{{b}}"
        home : "{{c}}"
    - name: Create files/dirs in users home dir
      file:
        name: "{{d}}"
        state: "{{e}}"
...

