---
- name: create user on all servers
  hosts: servers
  tasks: 
    - name: create user
      user:
        name: Anu
        password: "{{ 'intelliqit' | password_hash('sha512') }}"
        uid: 1234
        home: /tmp/Anu
        shell: /bin/bash
        comment: "normal User"
...
