---
- name: Implementing host scope variables
  hosts: 172.31.30.97
  tasks:
    - name: user creation
      user:
        name: "{{a}}"
        password: "{{b}}"
        uid: "{{c}}"
        home: "{{d}}"
        shell: "{{e}}"
        comment: "{{f}}"
...

