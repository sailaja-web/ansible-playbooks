---
- name: Install apache2
  apt:
    name: apache2
    state: present
    update_cache: yes
  when: ansible_facts.distribution == "Ubuntu" 
- name:
  yum: 
    name: httpd
    state: present
    update_cache: yes
  when: ansible_facts.distribution == "RedHat"
...
